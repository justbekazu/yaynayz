<div class="card mt-3" style="border: 0">
  <div class="card-body">
    <h5 class="card-title"><strong>{{title}}</strong></h5>
    <h6 class="card-subtitle mb-2 text-primary"><strong>Shortcode {{shortcode}}</strong></h6>
    <h6 class="text-secondary">by {{author.username}} on {{format_date created_at}}</h6>
    <br>
    <div id="voting-panel">
    <button class="btn btn-primary" id="btn-yay" value="true">YAY</button> <button class="btn btn-danger" id="btn-nay" value="false">NAY</button>
    </div>
    <div id="results-panel">
      This will go in a fancy chart
    <h6 class="card-subtitle mb-2 text-primary"><strong>Yay: {{yay}}</strong></h6>
    <h6 class="card-subtitle mb-2 text-primary"><strong>Nay: {{nay}}</strong></h6>
    </div>
  </div>
</div>